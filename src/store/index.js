const identify = require('./identify');
const user = require('./user');
const map = require('./map');
const libs = require('./libs');

module.exports = {
  identify: identify,
  user: user,
  libs: libs,
  map: map
};
